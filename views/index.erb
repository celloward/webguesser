<html>
  <head>
    <style>
      body {  
        background: <%= color %>;
      }
    </style>
  </head>
  <body>
    <h2>What number am I thinking of?</h2>
    <form>
    <% if result == "Correct!" %>
      You guessed it! The secret number is <%= secret_num %>
    <% elsif guesses == 0 %>
      Game over! You are out of guesses.
    <% else %>
      <p><em>Pick a number between 0 and 99</em></p>
      <input type="text" name="guess" autofocus="autofocus" />
      <input type="submit" value="Guess!" />
      <p>
        <%= result %>
        You have <%= guesses %> guesses left.
      </p>
    <% end %>
    </form>
    <% if result == "Correct!" || guesses == 0 %>
      <form method="post" action="/domethod" >
        <input type="submit" value="Play again!" autofocus="autofocus" s>
      </form>
    <% end %>  
  </body>
</html>